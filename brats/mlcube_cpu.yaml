name: MLCommons Brats
description: MLCommons Brats integration
authors: 
 - {name: "MLCommons Best Practices Working Group"}

platform:
  accelerator_count: 0

docker:
  # Image name.
  image: mlcommons/brats_cpu:0.0.1
  # Docker build context relative to $MLCUBE_ROOT. Default is `build`.
  build_context: "."
  # Docker file name within docker build context, default is `Dockerfile`.
  build_file: "Dockerfile_CPU"

tasks:
  example:
  # Run implementation
    parameters:
      inputs: {parameters_file: {type: file, default: parameters.yaml}}

  run:
  # Run implementation
    parameters:
      inputs: {input_folder: data/, parameters_file: {type: file, default: parameters.yaml}}
      outputs: {output_folder: data/results/}
