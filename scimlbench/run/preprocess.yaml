schema_type: mlcube_invoke
schema_version: 1.0.0

task_name: preprocess

input_binding:
        data_dir: $WORKSPACE/data

output_binding:
        data_dir: $WORKSPACE/data
        log_dir: $WORKSPACE/preprocess_logs
